query GetCategories($withSpots: Boolean!, $city: String) {
  categories(withSpots: $withSpots, city: $city) {
    emoji
    label
    slug
    spots @include(if: $withSpots) {
      description
      googleMapsUrl
      name
      slug
    }
  }
}
