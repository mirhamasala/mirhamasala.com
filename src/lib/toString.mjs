import { h } from 'hastscript'
import { toString } from 'hast-util-to-string'
import path from 'path'

const fileName = path.join(process.cwd(), 'src/lib/example.html')

// {* For title and excerpt *}
